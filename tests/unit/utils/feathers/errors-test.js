import feathersErrors from 'dummy/utils/feathers/errors';
import { module, test } from 'qunit';

module('Unit | Utility | feathers/errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = feathersErrors();
  assert.ok(result);
});
